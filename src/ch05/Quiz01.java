package ch05;

public class Quiz01 {
    public static void main(String[] args) {
        /*
        참조 타입에 대한 설명으로 틀린 것은 무엇입니까? 4
            ➊ 참조 타입에는 배열, 열거, 클래스, 인터페이스가 있다.
            ➋ 참조 타입 변수의 메모리 생성 위치는 스택이다.
            ➌ 참조 타입에서 ==, != 연산자는 객체 번지를 비교한다.
            ➍ 참조 타입은 null 값으로 초기화할 수 없다.
            -> 참조타입은 null로 초기화 가능하다.

              참조타입에는 배열,열거,클래스,인터페이스,문자형
             참조타입은 객체를 저장하는 타입, 데이터는 heap 영역에 저장
             참조타입은 비교연산자를 사용시 객체의 주소를 비교한다.
         */
    }
}
