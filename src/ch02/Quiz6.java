package ch02;

public class Quiz6 {
    public static void main(String[] args) {
        /*
        연산식의 타입 변환 중에서 컴파일 에러가 발생하는 것을 선택하고 그 이유
        byte byteValue = 10;
        float floatValue = 2.5F;
        double doubleValue = 2.5

         byte result = byteValue + byteValue;

          // 5가 int. 둘 중 하나 int 면 int 가능
         int result = 5 + byteValue;

         // 5가 int. floatValue 는 실수라서 float 로 받아서 문제X
         정수보다 실수의 허용범위가 더 크다
         -> 허용범위가 큰쪽으로 타입변환이 일어난다.
         float result = 5 + floatValue;

         // 5가 int. doubleValue 는 실수라서 double 로 받으니 문제 X
         double result = 5 + doubleValue;

        1번 :  int 로 받아야 컴파일 에러가 안뜬다
        int result = byteValue + byteValue;
        byte + byte = int 형으로 자동타입변환

         */
    }
}
