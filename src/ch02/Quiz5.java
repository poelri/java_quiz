package ch02;

public class Quiz5 {
    public static void main(String[] args) {
        /*
        강제 타입 변환에서 컴파일 에러가 발생하는 것을 선택하고 그 이유를 작성
        int intValue = 10;
        char charValue = 'A';
        double doubleValue = 5.7;
        String strValue = "A";


         double var = (double) intValue;
         byte var = (byte) intValue;
         int var = (int) doubleValue;
         char var = (char) strValue;

         4: char 에서는 큰따옴표를 쓰지 못한다.
         *String 은 클래스이고 char 는 2byte 로 정수로
         이루어진 기본형 타입(primitive type)중에 하나
         참조타입들은 모두 객체를 저장하는 타입입니다.
         String 은 참조타입이다~
         char 는 기본타입~




         */
    }
}
