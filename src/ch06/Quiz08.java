package ch06;

public class Quiz08 {
    public static void main(String[] args) {
        /*
인스턴스 멤버와 정적 멤버에 대한 설명으로 틀린 것은 무엇입니까? 2
➊ 정적 멤버는 static으로 선언된 필드와 메소드를 말한다.
➋ 인스턴스 필드는 생성자 및 정적 블록에서 초기화될 수 있다.
➌ 정적 필드와 정적 메소드는 객체 생성 없이 클래스를 통해 접근할 수 있다.
➍ 인스턴스 필드와 메소드는 객체를 생성하고 사용해야 한다

객체가 없어도 실행된다는 특징 때문에 정적 메소드와 정적 블록 내부에
인스턴스 필드나 인스턴스 메소드를 사용할 수 없다. 또한 this 키워드도 사용불가

         */
    }
}
